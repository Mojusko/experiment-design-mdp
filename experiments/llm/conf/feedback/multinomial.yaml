name: "multinomial"
num_policies: 2
num_components: 3
num_rounds: 7
lambda_reg: 100 # Default lambda, used if use_model_specific_lambda is false
use_model_specific_lambda: true # Set to false to use the lambda_reg above
adaptive_estimation_frequency: 0 
adaptive_estimation_start: 0
adaptive_design_frequency: 10 
#adaptive_design_frequency: 0 
pass_V: true

# Model-specific parameters (moved from experiment config)
model_specific_params:
  default:
    lambda_reg: 100 # Default lambda if model not specified below
  japanese-text:
    lambda_reg: 1
  japanese-image:
    lambda_reg: 100
  aesthetics:
    lambda_reg: 10
  random_combination:
     lambda_reg: 10
