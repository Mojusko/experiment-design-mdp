defaults:
  - config
  - override feedback: multinomial
feedback:
  num_rounds: 5
tester:
  params:
    N_test_prompts: 50
    N_pairs_eval: 150
experiment:
  episodes: 100
  vocab_size: 500
  #scorer_model: "art"

