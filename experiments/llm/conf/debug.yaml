defaults:
  - config
  - override feedback: multinomial
feedback:
  num_rounds: 5
tester:
  params:
    N_test_prompts: 150
    N_pairs_eval: 350
experiment:
  episodes: 10
  vocab_size: 100
  #scorer_model: "art"

