defaults:
  - config
  - override feedback: multinomial
feedback:
  num_rounds: 10
tester:
  params:
    N_test_prompts: 100
    N_pairs_eval: 250
experiment:
  episodes: 50
  vocab_size: 350
  #scorer_model: "art"

