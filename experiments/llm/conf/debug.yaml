defaults:
  - config
  - override feedback: multinomial
feedback:
  num_components: 10
  adaptive_estimation_frequency: 10
tester:
  params:
    N_test_prompts: 100
    N_pairs_eval: 250
experiment:
  episodes: 50
  vocab_size: 600
  #scorer_model: "art"

